"use strict"  //treats all javascripts code as the newer versions of code

// alert("hello") // it can't be runned on node js environment only be runned on front end or console


let name = "harsh"
let value = 18
let isLoggedIn = true;

/*data types -- 

number
bigint
string => ""
boolean => 0/1
null => standalone value
undefined =>
symbol => unique

*/
console.log("more changes to be done")
console.log(typeof undefined); //undefined
console.log(typeof null); //object

